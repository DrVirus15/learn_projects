Зміни в Assignment10Part1:
     - видалений весь закоментований код.
     - видалено всі зайві System.out
     - видалено OPERATORS_LIST та весь код який його використовував.
     - заповнення змінних в мапу variables винесено в окремий метод parseVariables(..)
     - метод main ловить винятки та виводить помилку або результат обчислення.
     - додано метод checkAndPushVariable(..) для заміни змінних в RPN
     - змінено метод calculateRPN:
            - змінено логіку витягування токенів з RPN та проведення обчислення
            - додано метод performTheOperation(..) який виконує арифметичну операцію для двох останніх чисел у стеку
            - додано викидання винятків
     - змінено метод parse:
            - додано isOperator(..) для перевірки символу на оператор
            - додано handleOperator(..) для обробки оператору
            - додано popOperatorsByPrecedence(..) для обробки стека операторів згідно з асоціативністю
                   - додано методи popPowerOperators(..) popDivMultiOperators(..) popPlusMinusOperators(..) для обробки
                     операторів по пріоритету.
                   - додано метод safePeek(..) для безпечного перегляду вершини стека операторів
                   - змінено перевірку асоціативності за допомогою методів: isPow isDivOrMulti isPlusOrMinus
                   - додано метод addOperatorToRPN(..) - виштовхує оператор зі стека в RPN і повертає наступний елемент
            - винесена обробка кінця виразу в метод handleEndOfExpression(..)
            - додано метод popRemainingOperators(..) для додавання всіх операторів до стека які залишились.
     - замінено LinkedList на ArrayDeque.
     - замінено виклик методу makeBaseOperation(..) та обробка операндів

Зміни в Assignment11Part1:
    - видалено класи-оператори які не відносяться до умови.
    - метод main ловить винятки та виводить помилку або результат обчислення.
    - видалено метод printResult(..)
    - додано метод calculate(..) який виконує обчислення вхідного виразу з урахуванням змінних.
    - замінено LinkedList на ArrayDeque.
    - метод setVariables(..) замінено на checkAndPushVariable(..) для коректної роботи з ArrayDeque.
    - змінено метод calculateRPN:
        - метод setVariables(..) перенесено в calculate(..) та змінено назву на checkAndPushVariable(..)
        - додано метод performTheOperation(..) який виконує арифметичну операцію для двох останніх чисел у стеку
        - видалено метод logCalculation(..)
    - змінено метод parse:
        - додано метод handleEndOfExpression(..) для обробки кінця виразу.
        - додано метод popRemainingOperators(..) для додавання всіх операторів до стека які залишились.
    - змінено метод handleOperator(..)
        - додано метод popOperatorsByPrecedence(..) для обробки стеку операторів згідно з пріоритетом.
        - додано метод handleCloseBracket(..) для обробки закриваючої дужки.

Зміни в Assignment13Part1:
    - видалено конструктор класу ImageLoader, тепер шлях до файлу передається в метод load(..) як параметр.
    - зміни в класі SilhouettesFinder:
        - видалено змінну image з конструктором, тепер зображення передається в метод countSilhouettes(..) як параметр,
          та передається в createSilhouettesMask(..).
        - змінено метод createSilhouettesMask(..), тепер він створює int масив пікселів і передає його в
          BackgroundFinder() та в Eraser() замість BufferedImage.
    - зміни в класі Eraser:
        - видалено змінну image з конструктором, тепер зображення не потрібне в класі, оскільки всі операції виконуються
          з масивом пікселів.
        - в методах separateSilhouettesMask(..) та findBackgroundMask(..) висота та ширина приходять параметром,
          щоб прибрати велику кількість викликів методів getWidth() та getHeight().
        - в методі findBackgroundMask(..) видалено виклик методу getRGB() для кожного пікселя, бо масив пікселів
          приходить параметром від separateSilhouettesMask(..).
    - зміни в класі SimilarPixelFinder:
        - прорахунок квадрата дельти змінено на ціле число для уникнення непотрібних перетворень
      між типами.
    - зміни в класі BackgroundFinder:
        - в методі CalculateColorFrequencies(..) видалено виклик getRGB() для кожного пікселя, бо масив пікселів
          приходить параметром від findBackground(..).
